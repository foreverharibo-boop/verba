# 베르바 v0.5.89 프롬프트 토큰 안내

아래 숫자는 **원문과 번역문을 제외한 지침·JSON 틀의 대략적인 입력 토큰**입니다. 현재 베르바가 실제로 만드는 프롬프트를 빈 예시 데이터로 생성한 뒤 `cl100k_base`로 계산했습니다. DeepSeek의 실제 토크나이저와는 차이가 날 수 있으므로 약 ±15% 범위로 보면 됩니다.

## 채팅 전체 번역

| 켠 기능 | 요청 1회당 약 | 기본 대비 | 쉽게 말하면 |
|---|---:|---:|---|
| 아무 맛도 없음 | 584토큰 | 기준 | 기본 번역·보존·형식·화자 범위 규칙 |
| 한캐의 맛 | 687토큰 | +103 | 한캐 말맛 규칙이 붙음 |
| 영캐의 맛 | 690토큰 | +106 | 영캐 말맛 규칙이 붙음 |
| 한캐 + 영캐 | 793토큰 | +209 | 두 문화 말맛 규칙이 함께 붙음 |
| 김홍진의 맛 기본값 | 770토큰 | +186 | 캐릭터 전용 말투와 대상 보호 규칙이 붙음 |
| 김홍진의 맛 모든 강도 높음 | 841토큰 | +257 | 강도·연령대·자칭 설정까지 붙음 |
| 미친 한출의 맛 | 573토큰 | -11 | 일반 기본 프롬프트를 더 짧은 전용 프롬프트로 교체 |
| 미친 한출 + 김홍진 모두 높음 | 806토큰 | +222 | 미친 한출 전용 규칙에 김홍진 말투만 추가 |
| 미친 한출 제외, 일반 말맛·관계·초보자 말투를 넉넉히 함께 사용 | 약 1,139토큰 | +555 | 여러 일반 설정이 모두 합쳐진 예시 |

`미친 한출의 맛`은 단독 모드입니다. 한캐·영캐·초보자 말투·관계 설정을 화면에서 켜 두어도 그 값은 저장만 되고 AI에는 전송되지 않습니다. 김홍진의 맛만 함께 전송됩니다. 따라서 모든 토글을 켠 상태의 실제 프롬프트는 마지막 일반 설정 묶음이 아니라 **미친 한출 + 김홍진 약 806토큰** 쪽입니다.

2분할은 같은 공통 지침을 두 요청에 각각 넣습니다. 예를 들어 기본 번역은 지침만 약 `584 × 2 = 1,168토큰`, 미친 한출 + 김홍진은 약 `806 × 2 = 1,612토큰`입니다. 원문은 둘로 나뉘지만 공통 지침은 두 번 처리되므로 짧거나 중간 길이 원문에서는 1분할이 더 빠를 수 있습니다.

## 다른 AI 요청

| 요청 종류 | 고정 지침 약 | 언제 사용하나 |
|---|---:|---|
| 내가 보내는 글 K→E | 312토큰 | 인풋 자동번역 |
| 선택 부분 재번역 | 521토큰 | 한 군데 다시 번역 |
| 여러 선택 부분 재번역 | 533토큰 | 여러 군데를 한 번에 다시 번역 |
| 이름 원문 찾기 | 76토큰 | 선택 이름이 애매할 때만 |
| 이름 과거 표기 찾기 | 89토큰 | 저장 번역에서 같은 이름의 옛 표기를 확인할 때만 |
| 호칭·역할 용어 계획 | 90토큰 | 반복되는 역할명이 감지됐을 때 |
| 호칭·역할 용어 교정 | 110토큰 | 실제 불일치 후보가 있을 때 |
| 품질 검수 | 621토큰 | 품질 검수를 켰고 로컬 검사에서 의심 구간이 잡혔을 때 |

화자 분류는 현재 베르바 내부 판별이 기본이라 별도 AI 프롬프트 비용이 없습니다. 이름·한영 병기·보호 표식의 정상화도 가능한 경우 베르바 내부에서 처리합니다. 표의 숫자에는 실제 원문, 번역 후보, 이름 목록, 사용자 직접 지침이 포함되지 않았으므로 실제 요청 입력량은 그 데이터만큼 늘어납니다.
